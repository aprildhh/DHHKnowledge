<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dhh.knowledge">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.RecyclerViewActivity" />
        <activity android:name=".activity.CustomControlActivity" />
        <activity android:name=".activity.FloatingActionButtonActivity" />
        <activity android:name=".activity.MySQLActivity" />
        <activity android:name=".activity.CVViewPagerActivity" />
        <activity android:name=".activity.XLKActivity"></activity>
    </application>

</manifest>